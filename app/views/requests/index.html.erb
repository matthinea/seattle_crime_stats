<%= render partial: 'form', 
  locals: { 
    precinct_options: @precinct_options, 
    beat_options: @beat_options,
    beat: @beat,
    precinct: @precinct,
    from_date: @from_date,
    to_date: @to_date
  } %>